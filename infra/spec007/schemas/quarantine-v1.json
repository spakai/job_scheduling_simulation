{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Spec007QuarantineV1",
  "type": "object",
  "required": ["schemaVersion", "handoffId", "sourceTopic", "sourcePartition", "sourceOffset", "createdAt", "request", "code"],
  "properties": {
    "schemaVersion": {"const": 1},
    "handoffId": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "sourceTopic": {"type": "string"},
    "sourcePartition": {"type": "integer", "minimum": 0},
    "sourceOffset": {"type": "integer", "minimum": 0},
    "createdAt": {"type": "string", "format": "date-time"},
    "request": {"type": "object"},
    "code": {"type": "string"}
  }
}
